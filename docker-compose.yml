services:
  leilao-front:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agrolance-front-container
    ports:
      - "4200:80"
    restart: always
    networks:
      - erpleilao_default

networks:
  erpleilao_default:
    external: true